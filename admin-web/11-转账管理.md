# 转账管理

> 返回 [后台管理前端目录](./README.md)

---

## 页面概述

- **页面路径**: `/admin/transfer`
- **权限要求**: Admin 或 Finance 角色
- **主要功能**: 批量转账批次管理、批次创建/编辑/执行、转账状态追踪

---

## 数据模型

### 转账用户

```typescript
interface TransferUser {
  uid: string;                // 用户UID
  phone: string;              // 手机号
  email: string;              // 邮箱
  amount: number;             // 转账金额
  currency: string;           // 币种
  transferStatus?: '待发放' | '发放中' | '已发放' | '失败';  // 发放状态
  txHash?: string;            // 交易哈希
  transferTime?: string;      // 发放时间
  failReason?: string;        // 失败原因
}
```

### 转账批次

```typescript
interface TransferBatch {
  id: string;                 // 批次ID
  name: string;               // 批次名称
  type: 'transfer' | 'payroll';  // 批次类型：普通转账 / 薪资发放
  createTime: string;         // 创建时间
  executeTime?: string;       // 执行时间
  totalUsers: number;         // 用户总数
  totalAmount: number;        // 总金额
  status: '草稿' | '待执行' | '执行中' | '已完成' | '失败';  // 批次状态
  users: TransferUser[];      // 用户列表
  successCount?: number;      // 成功数量
  failedCount?: number;       // 失败数量
}
```

### 批次类型说明

| 类型 | 标识 | 说明 | 适用场景 |
|------|------|------|----------|
| 普通转账 | transfer | 一般性批量转账 | 活动奖励、返利、退款等 |
| 薪资发放 | payroll | 员工薪资发放 | 月度工资、奖金、补贴等 |

---

## 批次生命周期

```
┌────────┐    ┌────────┐    ┌────────┐    ┌─────────────┐
│  草稿  │ →  │ 待执行  │ →  │ 执行中  │ →  │ 已完成/失败 │
└────────┘    └────────┘    └────────┘    └─────────────┘
   ↑              │
   └──────────────┘
    (可编辑/删除)
```

**状态说明**:

| 状态 | 说明 | 可执行操作 |
|------|------|------------|
| 草稿 | 新建未提交的批次 | 编辑、删除、提交 |
| 待执行 | 已提交等待执行 | 执行、撤回 |
| 执行中 | 正在执行转账 | 查看进度 |
| 已完成 | 全部转账完成 | 查看详情 |
| 失败 | 批次执行失败 | 查看详情、重试 |

---

## 页面布局

### 转账管理主页面结构

```
┌──────────────────────────────────────────────────────────────────────────┐
│  Header                                                                   │
├──────────┬───────────────────────────────────────────────────────────────┤
│          │  Breadcrumb: 首页 / 转账管理                                   │
│  Sidebar ├───────────────────────────────────────────────────────────────┤
│          │                                                                │
│          │  ┌──────────────────────────────────────────────────────────┐ │
│          │  │ 统计卡片                                                  │ │
│          │  │ [总批次: 45]  [待执行: 3]  [执行中: 1]  [今日完成: 8]    │ │
│          │  └──────────────────────────────────────────────────────────┘ │
│          │                                                                │
│          │  ┌──────────────────────────────────────────────────────────┐ │
│          │  │ 操作栏                                                    │ │
│          │  │ [状态▼] [日期范围] [搜索______]   [新建批次] [Excel导入] │ │
│          │  └──────────────────────────────────────────────────────────┘ │
│          │                                                                │
│          │  ┌──────────────────────────────────────────────────────────┐ │
│          │  │ │批次ID │名称      │用户数│总金额      │状态  │时间  │操作│ │
│          │  │ ├───────┼──────────┼──────┼────────────┼──────┼──────┼────┤ │
│          │  │ │B00123 │12月工资  │50    │500,000USDT │已完成│12-10 │[详]│ │
│          │  │ │B00124 │奖金发放  │20    │100,000USDT │待执行│12-10 │[执]│ │
│          │  │ │B00125 │测试批次  │5     │5,000 USDT  │草稿  │12-10 │[编]│ │
│          │  │ ...                                                       │ │
│          │  ├──────────────────────────────────────────────────────────┤ │
│          │  │                    < 1 2 3 4 5 ... 10 >                   │ │
│          │  └──────────────────────────────────────────────────────────┘ │
│          │                                                                │
└──────────┴───────────────────────────────────────────────────────────────┘
```

---

## 功能模块

### 1. 统计概览

**功能描述**: 显示转账批次相关的统计数据

**用户角色**: Admin / Finance

**UI 组件**:
- **StatisticCard**: 统计卡片

**统计项**:

| 统计项 | 说明 | 样式 |
|--------|------|------|
| 总批次 | 批次总数 | 蓝色 |
| 待执行 | 待执行状态批次数 | 橙色 |
| 执行中 | 正在执行的批次数 | 黄色 |
| 今日完成 | 今日完成的批次数 | 绿色 |

---

### 2. 批次列表筛选

**功能描述**: 筛选和搜索转账批次

**用户角色**: Admin / Finance

**筛选条件**:

| 字段 | 组件类型 | 选项 |
|------|----------|------|
| 批次类型 | Select | 全部、普通转账、薪资发放 |
| 状态 | Select | 全部、草稿、待执行、执行中、已完成、失败 |
| 日期范围 | DateRangePicker | 创建时间范围 |
| 关键词 | Input | 批次ID、批次名称 |

---

### 3. 批次列表展示

**功能描述**: 以表格形式展示批次列表

**用户角色**: Admin / Finance

**表格列定义**:

| 列名 | 字段 | 宽度 | 说明 |
|------|------|------|------|
| 批次ID | id | 100px | 批次唯一标识 |
| 批次类型 | type | 80px | 普通转账/薪资发放 |
| 批次名称 | name | 150px | 批次描述名称 |
| 用户数 | totalUsers | 80px | 转账用户数量 |
| 总金额 | totalAmount | 120px | 转账总金额 |
| 币种 | currency | 60px | 转账币种 |
| 状态 | status | 80px | 批次状态标签 |
| 创建时间 | createTime | 160px | 批次创建时间 |
| 操作 | - | 150px | 操作按钮 |

**类型标签**:

| 类型 | 颜色 | 说明 |
|------|------|------|
| 普通转账 | 蓝色 | 一般批量转账 |
| 薪资发放 | 紫色 | 薪资相关发放 |

**状态标签**:

| 状态 | 颜色 | 说明 |
|------|------|------|
| 草稿 | 灰色 | 未提交，可编辑 |
| 待执行 | 蓝色 | 等待执行 |
| 执行中 | 黄色 | 正在执行 |
| 已完成 | 绿色 | 执行完成 |
| 失败 | 红色 | 执行失败 |

**操作按钮**:

| 状态 | 可用操作 |
|------|----------|
| 草稿 | 编辑、删除、提交 |
| 待执行 | 执行、撤回、查看 |
| 执行中 | 查看进度 |
| 已完成 | 查看详情 |
| 失败 | 查看详情、重试 |

---

### 4. 创建批次

**功能描述**: 手动创建转账批次或通过 Excel 导入

**用户角色**: Admin / Finance

#### 4.1 手动创建

**触发方式**: 点击「新建批次」按钮

**创建批次对话框**:
```
┌────────────────────────────────────────────────────────────────┐
│  新建转账批次                                              [×]  │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  批次类型 *                                                    │
│  (○) 普通转账    (○) 薪资发放                                  │
│                                                                │
│  批次名称 *                                                    │
│  [________________________]                                    │
│                                                                │
│  转账币种 *                                                    │
│  [USDT              ▼]                                        │
│                                                                │
│  添加转账用户                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ │用户UID    │手机号        │邮箱           │金额       │ │  │
│  │ ├───────────┼──────────────┼───────────────┼───────────┤ │  │
│  │ │ [______]  │ [__________] │ [___________] │ [______]  │ │  │
│  │ │ [______]  │ [__________] │ [___________] │ [______]  │ │  │
│  │ │ [______]  │ [__________] │ [___________] │ [______]  │ │  │
│  │ │                                          [+ 添加行]  │ │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                │
│  用户数: 3      总金额: 15,000 USDT                           │
│                                                                │
│         [取消]    [保存草稿]    [提交执行]                     │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

#### 4.2 Excel 导入

**触发方式**: 点击「Excel 导入」按钮

**导入对话框**:
```
┌────────────────────────────────────────────────────────────────┐
│  Excel 导入转账                                            [×]  │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  批次名称 *                                                    │
│  [________________________]                                    │
│                                                                │
│  上传文件                                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                                                          │  │
│  │        📄 点击或拖拽文件到此处上传                        │  │
│  │                                                          │  │
│  │           支持 .xlsx, .xls 格式                          │  │
│  │                                                          │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                │
│  [下载模板]                                                    │
│                                                                │
│  模板格式说明:                                                 │
│  • 列: UID, 手机号, 邮箱, 金额, 币种                          │
│  • 金额使用数字格式，不含千位分隔符                           │
│                                                                │
│         [取消]    [上传并解析]                                 │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

**导入结果预览**:
```
┌────────────────────────────────────────────────────────────────┐
│  导入预览                                                  [×]  │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  解析结果: 50 条记录，48 条有效，2 条错误                     │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ │行号│UID      │手机号       │金额      │状态          │ │  │
│  │ ├────┼─────────┼─────────────┼──────────┼──────────────┤ │  │
│  │ │ 1  │10001    │138****1234  │5,000     │✓ 有效        │ │  │
│  │ │ 2  │10002    │139****5678  │3,000     │✓ 有效        │ │  │
│  │ │ 3  │99999    │-            │2,000     │✗ 用户不存在  │ │  │
│  │ │ ...                                                   │ │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                │
│  有效用户: 48      总金额: 480,000 USDT                       │
│                                                                │
│  [✓] 忽略错误记录，仅导入有效数据                             │
│                                                                │
│         [取消]    [确认导入]                                   │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

### 5. 执行批次

**功能描述**: 执行待执行状态的转账批次

**用户角色**: Admin / Finance

**触发方式**: 点击批次行的「执行」按钮

**执行确认对话框**:
```
┌────────────────────────────────────────┐
│  执行转账批次                      [×]  │
├────────────────────────────────────────┤
│                                        │
│  确定要执行此转账批次吗？              │
│                                        │
│  批次ID:   B00124                      │
│  批次名称: 奖金发放                    │
│  用户数:   20                          │
│  总金额:   100,000 USDT                │
│                                        │
│  ⚠️ 执行后将向 20 个用户发起转账      │
│  请确认信息无误后再执行                │
│                                        │
│         [取消]    [确认执行]           │
│                                        │
└────────────────────────────────────────┘
```

---

### 6. 批次详情

**功能描述**: 查看批次详细信息和用户级转账状态

**用户角色**: Admin / Finance

**触发方式**: 点击批次行的「详情」按钮

**详情抽屉**:
```
┌────────────────────────────────────────────────────────────────┐
│  批次详情                                                  [×]  │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  基本信息                                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 批次ID:     B00123                                       │  │
│  │ 批次名称:   12月工资                                     │  │
│  │ 创建时间:   2025-12-10 10:00:00                          │  │
│  │ 执行时间:   2025-12-10 14:30:00                          │  │
│  │ 状态:       已完成 ✓                                     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                │
│  执行统计                                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 用户总数:   50                                           │  │
│  │ 总金额:     500,000 USDT                                 │  │
│  │ 成功:       48 (96%)  ████████████████████░░             │  │
│  │ 失败:       2 (4%)                                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                │
│  用户明细                                          [仅显示失败]│
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ │UID    │手机号      │金额      │状态    │交易哈希│时间  │ │  │
│  │ ├───────┼────────────┼──────────┼────────┼────────┼──────┤ │  │
│  │ │10001  │138****1234 │10,000    │已发放✓ │0x123...│14:31 │ │  │
│  │ │10002  │139****5678 │8,000     │已发放✓ │0xabc...│14:32 │ │  │
│  │ │10003  │137****9012 │12,000    │失败✗   │-       │-     │ │  │
│  │ │       │            │          │余额不足│        │      │ │  │
│  │ ...                                                      │ │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                │
│  [导出明细]                                                    │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

**用户转账状态**:

| 状态 | 颜色 | 说明 |
|------|------|------|
| 待发放 | 灰色 | 等待执行 |
| 发放中 | 蓝色 | 正在处理 |
| 已发放 | 绿色 | 转账成功 |
| 失败 | 红色 | 转账失败，显示失败原因 |

---

## UI 组件列表

| 组件名称 | 类型 | 功能说明 |
|----------|------|----------|
| TransferStats | Card | 统计概览卡片 |
| BatchFilter | Form | 批次筛选表单 |
| BatchTable | Table | 批次列表表格 |
| CreateBatchModal | Modal | 创建批次对话框 |
| ExcelImportModal | Modal | Excel 导入对话框 |
| ImportPreview | Table | 导入预览表格 |
| ExecuteConfirmModal | Modal | 执行确认对话框 |
| BatchDetailDrawer | Drawer | 批次详情抽屉 |
| UserTransferTable | Table | 用户转账明细表格 |
| StatusTag | Tag | 状态标签 |
| ProgressBar | Progress | 执行进度条 |

---

## 状态与交互

### 页面状态

| 状态 | 描述 | UI 表现 |
|------|------|---------|
| Loading | 列表加载中 | 显示表格骨架屏 |
| Empty | 无批次数据 | 显示空状态 |
| Executing | 正在执行批次 | 显示进度条 |
| Error | 加载失败 | 显示错误提示 |

### 用户操作响应

| 操作 | 成功响应 | 失败响应 |
|------|----------|----------|
| 创建批次 | Toast "批次创建成功"，刷新列表 | Alert 显示错误原因 |
| Excel 导入 | Toast "导入成功，共 X 条"，刷新列表 | Alert 显示解析错误 |
| 执行批次 | Toast "批次已开始执行"，状态变更 | Alert 显示错误原因 |
| 删除批次 | Toast "批次已删除"，刷新列表 | Alert 显示错误原因 |

---

## 排序与分页

**排序字段**: 创建时间、批次状态、总金额、用户数、类型（薪资优先）。

**分页**: 显示总数、每页 20/50/100、支持跨页选择用于批量删除/导出（仅限草稿/待执行）。

---

## 导出范围

- 支持导出“筛选后的集合”或“全部批次”；导出明细可选仅失败/全部用户。

## 权限控制

| 功能 | Admin | Finance |
|------|-------|---------|
| 查看批次列表 | ✓ | ✓ |
| 创建批次 | ✓ | ✓ |
| 编辑批次 | ✓ | ✓ |
| 删除批次 | ✓ | - |
| 执行批次 | ✓ | ✓ |
| 查看批次详情 | ✓ | ✓ |
| 导入 Excel | ✓ | ✓ |
| 导出明细 | ✓ | ✓ |

---

## 数据导出

### Excel 模板格式

| 列名 | 必填 | 说明 |
|------|------|------|
| UID | 是 | 用户唯一标识 |
| 手机号 | 否 | 用户手机号（用于校验） |
| 邮箱 | 否 | 用户邮箱（用于校验） |
| 金额 | 是 | 转账金额（数字格式） |
| 币种 | 是 | 转账币种（USDT等） |

### 导出明细格式

导出的批次明细包含以下字段：
- 批次ID、批次名称
- 用户UID、手机号、邮箱
- 转账金额、币种
- 转账状态、交易哈希
- 发放时间、失败原因

---

> 返回 [后台管理前端目录](./README.md)
