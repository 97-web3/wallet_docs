# 区块链网络规格 - 公共规格文档

## 1. 文档标识

| 属性 | 值 |
|------|-----|
| 文档ID | COMMON-003 |
| 文档名称 | 区块链网络规格 |
| 版本 | 1.0 |
| 更新日期 | 2025-12-08 |
| 适用范围 | 中心化钱包、去中心化钱包 |

---

## 2. V1.0 支持的区块链网络

### 2.1 网络列表

| 网络名称 | 代码 | 代币标准 | 地址格式 | 地址示例 |
|----------|------|----------|----------|----------|
| **Tron** | TRX | TRC-20 | T 开头，34 位字符 | `T9yD14Nj9j7xAB4dbGeiX9h8unkKHxuWwb` |
| **BNB Smart Chain** | BNB | BEP-20 | 0x 开头，42 位字符 | `0x742d35Cc6634C0532925a3b844Bc9e7595f8bDc7` |
| **Ethereum** | ETH | ERC-20 | 0x 开头，42 位字符 | `0x742d35Cc6634C0532925a3b844Bc9e7595f8bDc7` |

### 2.2 网络显示规范

| 属性 | TRX | BNB | ETH |
|------|-----|-----|-----|
| 显示名称 | Tron (TRC20) | BNB Chain (BEP20) | Ethereum (ERC20) |
| 简称 | TRX | BNB | ETH |
| 图标 | tron_icon.svg | bnb_icon.svg | eth_icon.svg |
| 主题色 | #FF0013 | #F3BA2F | #627EEA |

---

## 3. 地址验证规则

### 3.1 地址格式验证

```javascript
/**
 * 验证地址格式是否符合所选网络
 * @param {string} address - 钱包地址
 * @param {string} network - 网络代码: 'TRX' | 'BNB' | 'ETH'
 * @returns {Object} { valid: boolean, error?: string }
 */
function validateAddress(address, network) {
  if (!address || typeof address !== 'string') {
    return { valid: false, error: '请输入地址' };
  }

  const trimmed = address.trim();

  switch (network) {
    case 'TRX':
      // TRX 地址: T 开头，34 位字符，Base58 编码
      if (!/^T[1-9A-HJ-NP-Za-km-z]{33}$/.test(trimmed)) {
        return { valid: false, error: 'TRX 地址格式错误' };
      }
      break;

    case 'BNB':
    case 'ETH':
      // ETH/BNB 地址: 0x 开头，42 位字符，十六进制
      if (!/^0x[a-fA-F0-9]{40}$/.test(trimmed)) {
        return { valid: false, error: `${network} 地址格式错误` };
      }
      break;

    default:
      return { valid: false, error: '不支持的网络' };
  }

  return { valid: true };
}
```

### 3.2 ETH/BNB 地址二次确认

由于 ETH 和 BNB 使用相同的地址格式，需要进行二次确认：

```
┌────────────────────────────────────────────────────────┐
│                    网络确认                              │
├────────────────────────────────────────────────────────┤
│                                                        │
│   ⚠️ 重要提示                                          │
│                                                        │
│   您选择的网络是: [ETH/BNB Chain]                       │
│                                                        │
│   ETH 和 BNB Chain 使用相同的地址格式。                  │
│   请确认收款地址属于正确的网络。                          │
│                                                        │
│   发送到错误网络可能导致资产永久丢失！                    │
│                                                        │
│   ┌──────────────────────────────────────────────┐    │
│   │ ☐ 我确认该地址属于 [网络名] 网络              │    │
│   └──────────────────────────────────────────────┘    │
│                                                        │
├────────────────────────────────────────────────────────┤
│         [ 取消 ]              [ 确认 ]                  │
└────────────────────────────────────────────────────────┘
```

**规则:**
- 用户必须勾选确认复选框才能点击"确认"按钮
- 未勾选时"确认"按钮保持禁用状态

---

## 4. 网络选择器组件

### 4.1 UI 规格

```
┌────────────────────────────────────────┐
│            选择网络                     │
├────────────────────────────────────────┤
│                                        │
│  ┌────────────────────────────────┐   │
│  │  🔴 Tron (TRC20)               │   │
│  │     适用于 TRX 网络的地址        │   │
│  └────────────────────────────────┘   │
│                                        │
│  ┌────────────────────────────────┐   │
│  │  🟡 BNB Chain (BEP20)          │   │
│  │     适用于 BSC 网络的地址        │   │
│  └────────────────────────────────┘   │
│                                        │
│  ┌────────────────────────────────┐   │
│  │  🔵 Ethereum (ERC20)           │   │
│  └────────────────────────────────┘   │
│                                        │
└────────────────────────────────────────┘
```

### 4.2 交互行为

| 交互 | 行为 |
|------|------|
| 点击网络选项 | 选中该网络，返回上级页面 |
| 选中状态 | 显示 ✓ 图标 |
| 默认选中 | 上次使用的网络 / 首次使用选 TRX |

---

## 5. 扩展性设计

### 5.1 网络配置结构

系统使用配置文件管理支持的网络，便于未来扩展：

```javascript
const SUPPORTED_NETWORKS = [
  {
    code: 'TRX',
    name: 'Tron',
    displayName: 'Tron (TRC20)',
    tokenStandard: 'TRC-20',
    addressRegex: /^T[1-9A-HJ-NP-Za-km-z]{33}$/,
    addressPrefix: 'T',
    icon: 'tron_icon.svg',
    color: '#FF0013',
    enabled: true,
    // 扩展字段
    explorerUrl: 'https://tronscan.org',
    rpcUrl: 'https://api.trongrid.io',
  },
  {
    code: 'BNB',
    name: 'BNB Smart Chain',
    displayName: 'BNB Chain (BEP20)',
    tokenStandard: 'BEP-20',
    addressRegex: /^0x[a-fA-F0-9]{40}$/,
    addressPrefix: '0x',
    icon: 'bnb_icon.svg',
    color: '#F3BA2F',
    enabled: true,
    explorerUrl: 'https://bscscan.com',
    rpcUrl: 'https://bsc-dataseed.binance.org',
  },
  {
    code: 'ETH',
    name: 'Ethereum',
    displayName: 'Ethereum (ERC20)',
    tokenStandard: 'ERC-20',
    addressRegex: /^0x[a-fA-F0-9]{40}$/,
    addressPrefix: '0x',
    icon: 'eth_icon.svg',
    color: '#627EEA',
    enabled: true,
    explorerUrl: 'https://etherscan.io',
    rpcUrl: 'https://mainnet.infura.io/v3/...',
  },
];
```

### 5.2 添加新网络步骤

1. 在配置文件中添加新网络配置项
2. 添加对应的网络图标资源
3. 实现地址验证逻辑
4. 配置 RPC 节点和区块浏览器 URL
5. 测试充值、提现、转账功能

---

## 6. 手续费配置

### 6.1 预估手续费显示

| 网络 | Gas 单位 | 备注 |
|------|----------|------|
| TRX | 能量/带宽 | 消耗账户能量或带宽 |
| BNB | Gwei | BNB 计价 |
| ETH | Gwei | ETH 计价 |

### 6.2 手续费获取

- 实时从链上获取当前 Gas 价格
- 显示预估手续费金额
- 手续费单位与所选网络原生代币一致

---

## 7. 验收标准

- [ ] 仅显示 TRX、BNB、ETH 三个网络选项
- [ ] 地址格式验证符合各网络规范
- [ ] ETH/BNB 地址输入后显示网络确认弹窗
- [ ] 网络选择器正确高亮当前选中网络
- [ ] 网络配置支持热更新/扩展

---

*文档创建日期: 2025-12-08*
